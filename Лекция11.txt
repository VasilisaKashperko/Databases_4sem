CTE - common execution (например, with)
с локальными view
могут быть рекурсивные

with x as
(select 1...n
union all
select n+1 from x where n<5)
select * from x

//
1
2
3
4
5

** генерация чего-либо (чисел, дат) с помощью запроса!!!

--------------------------------------------------------

функции не работают с бд, они умеют возвращать не только скалярное значение,
но и табличные)
inline - табличные 
нельзя в бд добавлять, можно в переменную

либо передаваемой переменной, либо сам себе!!!!!!













